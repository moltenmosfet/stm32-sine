# Fork notes — moltenmosfet/stm32-sine (dyno absorber)

This is the Molten MOSFET dyno fork of Open Inverter's `stm32-sine`, driving a
Nissan Leaf EM57 motor as the absorber on a chassis dynamometer. Rationale for
individual changes is in the per-task rows below; deeper design rationale that
isn't captured here lives in internal review notes (not part of this repo).

## Two repos, two forks

`stm32-sine` vendors `libopeninv` as a submodule; each has its own upstream and fork:

| Repo | Upstream (`origin`) | Fork (`fork`) | Baseline pin |
|---|---|---|---|
| superproject | jsphuebner/stm32-sine | moltenmosfet/stm32-sine | `1dfab85` |
| libopeninv    | jsphuebner/libopeninv | moltenmosfet/libopeninv | `78e3f72` |

`libopencm3` submodule is left at its pin (`5ba1bb5`); not forked.

All review line numbers refer to these two pins. `fixes` is the integration branch
in each repo, created off the pin and pushed to `fork`. Task branches (`fix/T<nn>-<slug>`)
fork off `fixes` and merge back after review.

### libopeninv submodule discipline
A task touching `libopeninv/` commits inside `libopeninv/`, pushes that branch to the
libopeninv fork, then bumps the submodule pointer in the superproject branch. Never let
a superproject branch reference a libopeninv SHA that isn't pushed to the fork.

## Upstream PR policy
`origin` stays pointed at upstream in both repos. `[UPSTREAM]` tasks become clean
branches off *current* upstream master at submission time (rebase then; develop against
the pin now). `[FORK]` tasks encode dyno policy and stay on the fork. Never push to
`origin` (jsphuebner) — forks only.

PR sequence: tasks marked UPSTREAM in the table below (T2, T3, T6's race-condition half,
T8, T9, T10, T11, T12, T15, T16, T17, T18, T19) are submitted as separate PRs, library-level
fixes in libopeninv going out before the superproject fixes that depend on them. Tasks
marked FORK (T1, T4, T5, T6's taper half, T7, T13, T14, T20) stay on the fork; T5
(deadtime compensation) may be offered upstream once bench-validated.

## Findings glossary

Each finding below (F1–F24) came out of a source-level review of the FOC control path,
the CAN/SDO stack, the scheduler, and the SINE build; F25 was found while porting the
upstream `35=AutoTune` branch. Task rows in the table further down reference these
numbers.

| # | What the defect is |
|---|---|
| F1 | Below about 450 rpm, the q-axis voltage clamp only allows one polarity, switched instantly with no hysteresis or ramp — kills braking/regen at low speed and spikes current at the threshold. |
| F2 | The PI controller's integral term updates in coarse steps (~1.7% of full modulation) at default gains because an integer division truncates before scaling — causes a low-torque limit cycle. |
| F3 | Phase-current sampling free-runs unsynchronized to the PWM switching, so samples land at random points on the current ripple, adding noise that's significant at partial load. |
| F4 | Below about 42 rpm the frequency estimate is forced to zero and the last direction stays latched — regen torque cuts on/off abruptly and direction can flap during near-standstill oscillation. |
| F5 | The anti-cogging feedforward (off by default) has a 16-bit angle wraparound glitch and an unfiltered amplitude estimate that can self-reinforce; only matters if the feature is turned on. |
| F6 | Lowering the modulation-limit parameter while the motor is running can make an internal limit calculation go negative under unsigned arithmetic, producing a bogus limit and an overmodulation spike. |
| F7 | An unused variant of the PI controller (present in the shared library, not used by this firmware) truncates its output to an integer — would quantize control for any other firmware built on the same library. |
| F8 | Reviewed and cleared: several suspected issues (double application of a timing-advance term, anti-windup interaction with dynamic clamps, overflow risk in voltage math, short-pulse suppression) turned out not to be bugs. |
| F9 | Configuration traps for this motor: pole-pair ratio must be an integer, an MTPA gain is off by default on a salient machine, the regen voltage taper must sit above the resistor-dump voltage, overcurrent thresholds are averaged across both current sensors, and the manual torque test parameters bypass throttle-path safety derates. |
| F10 | Switching dead-time is never compensated in the modulation, producing a voltage error large enough to distort control at low torque and low current. |
| F11 | CAN commands to reload saved parameters or restore defaults aren't blocked while the motor is running — a remote command mid-run can switch encoder mode and scramble the control loop. A separate remote-start path also skips the contactor-close interlock. |
| F12 | Three bugs in the CAN receive filter setup: a leftover-filter flush uses the wrong index (silently drops an extended-ID filter), mask banks are checked against the wrong per-bank count (wastes half of every mask bank), and an incompletely filled bank accidentally accepts CAN ID 0. |
| F13 | A counter shared between the fast PWM interrupt and the slower frequency-update interrupt can be read and cleared non-atomically, occasionally losing an angle increment and undercounting frequency. |
| F14 | Writing most parameters over CAN triggers a full reconfiguration routine inside the CAN receive interrupt, adding tens of microseconds of latency per write — noticeable for high-rate commands like a torque setpoint. |
| F15 | If a scheduled task overruns its period, the 16-bit hardware timer compare can end up behind the counter, and the task then silently stalls for up to roughly 650 ms before it fires again. |
| F16 | Minor items grouped together: a fixed-point filter with a small negative rounding bias, one filter's state shared by two callers running it at twice the intended rate, an ADC-channel-count off-by-one on one hardware variant, a self-correcting startup transient in a cogging estimate, missing length checks on incoming CAN SDO frames, and a start interlock that doesn't count a regen (negative) command as "throttle pressed." |
| F17 | A 2023 fix for current-magnitude overflow above ~1000 A was added to the math library but never wired into the function it was meant to fix — the overflow is still present. |
| F18 | The terminal "defaults" command resets parameter values in memory but never triggers the recalculation that applies them, so the reset has no effect until an unrelated parameter write happens to trigger it. |
| F19 | The terminal "start" command runs a fixed-point conversion on a plain integer, shifting most mode values down to "off" — the command silently does nothing for most inputs. |
| F20 | An RMS calculation divides by a value derived from frequency with no floor, dividing by zero below 1 Hz stator frequency. |
| F21 | A logic condition in sine-wave current processing uses OR where AND was intended, making it always true; currently harmless because that code path isn't reached in the affected mode. |
| F22 | Angle interpolation between pulses on single-channel encoders isn't clamped, so it can overshoot past the next real pulse and then snap back — a sawtooth in the synthesized angle. |
| F23 | A regen safety guard meant to block driving in the wrong direction compares a value against itself in single-channel encoder mode, so it can never trigger — looks like protection, does nothing. |
| F24 | Documentation/config traps for all users: a negative overcurrent-limit parameter silently inverts the trip thresholds on non-Prius hardware instead of disabling the limit; the deadtime parameter is a nonlinear hardware register code, not a linear time value; two output modes stay nominally enabled in the FOC build without actually driving anything; and current-limiting has undocumented floors below which it can't act. |
| F25 | In the upstream `35=AutoTune` branch, the bidirectional test-spin's averaged sync-error output can never publish: the test angle always steps in twos (even values only) but the pass-end check compares against an odd endpoint, so the condition is unreachable and the intermediate half-average is silently overwritten every revolution instead. |

## Applied tasks

Branch naming `fix/T<nn>-<slug>`, merged `--no-ff` into `fixes` in each repo.
Verification: host suite = `cd test && make && ./test_sine`; firmware = `make
CONTROL=FOC` and `make CONTROL=SINE` (ARM toolchain became available mid-wave-2,
so all firmware-only changes are now compile-checked).

| Task | Type | Repo(s) | Verification | Status |
|---|---|---|---|---|
| T0 | FORK | both | n/a | done — forks, remotes, fixes, this file |
| T1 | — | superproject | host suite | done — picontroller/foc under test, libopencm3 stubs |
| T2 | UPSTREAM | libopeninv (+super bump) | host tests (3) + FOC/SINE build | done — F2/F7 |
| T3 | UPSTREAM | libopeninv + superproject | host test + FOC build | done — F6 (GetQLimit host-tested; UpdateVoltageLimits firmware-built) |
| T7 | FORK | superproject | FOC/SINE build + host | done — F14 (guarded #if CTRL_FOC; fast-paths `manualid`/`manualiq` so CAN torque commands skip the reconfig cost) |
| T8 | UPSTREAM | libopeninv (+super bump) | review + FOC build | done — F11 (review-verified; an automated test harness for this path wasn't cost-effective) |
| T15 | UPSTREAM | superproject | host test + SINE/FOC build | done — F17 |
| T17 | UPSTREAM | superproject | FOC/SINE build + host | done — F19/F20/F21 (3 commits) |
| T4 | FORK | superproject | host tests (7) + FOC/SINE build | done — F1 (QClamp policy class, `qlimfrq` param id 165, 0 = dyno mode / restriction off; shrinking qlimit clamps instantly; dead QLIMIT_FREQUENCY macro removed) |
| T5 | FORK→UPSTREAM cand. | libopeninv + superproject | host tests (4) + FOC/SINE build | done — F10 (`dtcomp` param id 166, default 0 = off; signs captured in ParkClarke, applied in InvParkClarke before short-pulse suppression; 2 A deadband; pinswap resolved at call site) |
| T6 | UPSTREAM (race) / FORK (taper) | superproject | host tests (4) + FOC/SINE build | done — F13/F4 (atomic copy+subtract of turnsSinceLastSample under cm_disable_interrupts, both encoder-mode branches; RegenTaperHold holds last taper factor 500 ms across the zero-freq deadband; separate commits for the PR split) |
| T11 | UPSTREAM | superproject | host test (all 65536 angles) + FOC/SINE build | done — F5 (AntiCogTriangle helper, 32-bit fold; estimator caveat documented, estimator fix out of scope) |
| T18 | UPSTREAM | superproject | review + FOC/SINE build | done — F22 (interpolation clamp) + F23 (SINGLE-mode direction-assumed comment) |
| T19 | UPSTREAM | superproject | review + FOC/SINE build | done — F24 (ocurlim ABS guard; deadtime DTG nonlinearity documented as comment — PARAM_ENTRY has no description field) |
| T9 | UPSTREAM | libopeninv (+super tests/bump) | host tests (4-combo matrix, exercises production code) + FOC/SINE build | done — F12 (all 3 defects fixed; pure packing logic split to canfilterpack.cpp so host tests drive real code — PR 5 may inline the split back if upstream prefers) |
| T10 | UPSTREAM | libopeninv (+super tests/bump) | host tests (4: normal, 1.5-period overrun, both wrap directions) + FOC/SINE build | done — F15 (missed deadline resyncs `TIM_CCR = counter + period` via `CheckOverrun`, a pure static split out for host testability — T9 precedent, raw TIM_CCR MMIO isn't host-drivable; overrun counter readable via `GetOverrunCount()`, no param) |
| T12 | UPSTREAM | both | host test (throttle IIR state isolation) + FOC/SINE build; items 2–4 review-verified | done — F16, 4 commits: throttle IIR state per caller (`FrequencyLimitCommandFw`, shared `RunFrequencyLimit` helper); `bmwAdcNextChan` wraps at `maxChan-1`; SDO frames DLC<8 rejected (libopeninv); cogging sentinels init 0 (first-crossing artifact; post-crossing resets keep ±INT32_MAX — a sample always lands between crossings). IIRFILTER rounding bias left unfixed — a metrology-polish item, not this pass's priority |
| T16 | UPSTREAM | both | review + host regression + FOC/SINE build (terminal glue, no host harness) | done — F18: terminal `defaults` now calls `Param::Change(PARAM_LAST)` (mirrors SDO path) and both `defaults` (super) and `load` (libopeninv) gate on `saveEnabled` via new `IsSaveEnabled()` accessor; messages match the `save` gate pattern |
| T13 | FORK (doc) | superproject | design doc only, no code | done — F3: `doc_sync_sampling_design.md`. Recommends TIM1_CC4 (JEXTSEL=1) injected trigger, Option A time-share (PWM ISR reads currents → hands injected group to resolver; JEOC ISR reads sin/cos → hands back) first, Option B (TIM1-locked excitation, single 2-deep sequence) as end-state. `syncadv`/`syncofs` re-tune required after A. Implementation gated on doc review |
| T14 | FORK (doc) | n/a | doc only; params cross-checked vs `param_prj.h` on fixes | done — parameter baseline for the EM57 build, written up as an internal doc (not part of this repo). Headline: `respolepairs=4 [VERIFY]` is the #1 first-spin trap (default 1 → 4× angle); `qlimfrq=0` + supervisory re-own of ALL throttle derates (F9) = the dyno mode; bus ladder 300/320/‹350 OBC›/‹360–375 dump›/385/430/‹450 HW›; `ocurlim`/`fmax`/ladder final values gate on HV power-stage selection |
| T20 | FORK (doc) | both | doc only, no code | done — public-facing README banners in both repos (this fork's purpose, the fixes/pin scheme, and the bench-validation disclosure) plus this rewrite of FORK_NOTES.md for outside readers |
| B2 | FORK (bench branch, **unmerged**) | superproject | host suite + FOC/SINE build; **NO HW PASS** | on `feat/B2-autotune` — port of upstream `35=AutoTune` (Pete's commissioning test modes via Jamie Jones, tip 1797847, upstream-untested): resolver check / phase check with inductance estimate / fwd+bidir test spins with sync-error spot values. Params renumbered to 167–171, values 2058–2060 (upstream's ids collided). Fixed F25 en route; added the missing entry path (`manualstart` + `testmode`≠0 → ManualRun via the normal start flow; physical start input always wins) and a `testres` terminal command for the phase-check verdict. Deliberately NOT merged to `fixes`: commissioning tooling, stock behavior unaffected only because the branch is separate |
| C1 | FORK (opt-in flag, **unmerged**) | superproject | flag-off byte-identity vs `fixes` (FOC + SINE, md5/cmp, independently re-verified) + host suite + flag-on FOC link + deliberate `#error` on SINE+flag; **NO HW PASS** | on `feat/C1-sync-sampling` — Option C.1 of `doc_sync_sampling_design.md` (finding **F3**), behind `#ifdef SYNC_CURRENT_SAMPLING` (`make … SYNC_SAMPLING=1`), default OFF: il1/il2 move to ADC2's injected group {IN5 dummy, IN5, IN8} triggered by TIM1_CC4 (OC4 PWM2, CCR4 = pwmmax−252, once-per-period OC4REF rising edge — bench-verified); AnaIn collapses to the library's single-ADC scan; resolver sin/cos both on ADC1 ranks 2/3 (pinswap becomes a rank pair, physical-pin defaults preserved); DUALMOD stays independent. HW_REV1 unsupported under the flag (il2-on-PA6 collides with resolver sin). Commissioning [VERIFY]: UEV top-vs-bottom RCR phase (sets CCR4 placement), then the doc §Q4 A/B noise-floor plan |
| T21 | FORK (opt-in flag, **merged to `fixes` 2026-07-19**) | superproject | flag-off byte-identity vs `fixes` (FOC + SINE, md5/cmp) + host suite (5 new cases) + flag-on FOC/SINE build; **NO HW PASS** | on `feat/T21-manualiq-autozero` — `manualiq` auto-zero on command-silence (D4 backstop), behind `#ifdef MANUALIQ_CMD_TIMEOUT` (`make CMD_TIMEOUT=1`), default OFF, FOC-only. Defence-in-depth BEHIND the GPIO dead-man: a bounded silence of the `manualiq`/`manualid` SDO command (stamped in T7's `Param::Change` fast-path) zeroes `manualiq` — a graceful command decay, NOT a fault trip (contactor/PWM-enable untouched, so a brief host stall recovers with no re-precharge). Timeout = param `iqtimeout` (id 172 [skips B2's 167-171], 10 ms ticks, 0=disabled, default 20=`PLACEHOLDER_HW`); detection semantics + timeout value are bench-gated → `PLACEHOLDERS.md` P1/P2. Decision logic split pure into `include/cmdtimeout.h` (`CmdTimeout`, RegenTaperHold/CheckOverrun precedent) so the host suite drives real code — `ApplyZero` zeroes the command past timeout with a contactor proxy untouched, + disabled/not-seen/refresh/wraparound guards; the FOC-only Param glue is firmware-compile-checked (host suite links the SINE param branch where `manualiq`/`iqtimeout` don't exist, same reason T7's manualiq path has no host test). `iqtimeout` added via an empty-expanding `CMD_TIMEOUT_PARAMS` macro (no `#ifdef` legal inside the `PARAM_LIST` backslash-macro). Amendment 500603a (ruled 2026-07-19): `manualid` zeroed too on expiry — a dead host must not leave flux current latched. Merged to `fixes` 973ce77 after full re-verification (flag-off byte-identical both variants, flag-on SINE inert, host suite green); flag default OFF keeps unvalidated behaviour off the default binary until bench validates P1/P2 |

| B4 | FORK (**merged to `fixes` 2026-07-20**) | superproject | FOC/SINE build (not byte-identical — behaviour change, expected); host suite green (unaffected — firmware `main`); scheduler T10 path covered by existing `test_stm32scheduler`; **NO HW PASS** | on `feat/B4-spread-can-tx` — port of upstream PR #58 (Modellfan, "spread mapped CAN TX slots"). Replaces the burst `canMap->SendAll()` in Ms10/Ms100Task with a `SendByIndex(canMapTxSlot)` round-robin (`canMapTxSlot = (canMapTxSlot+1) % MAX_MESSAGES`), one slot per tick, and adds a new `Ms1Task`. Mapping: 10 ms-period map → one slot per **1 ms** tick (new Ms1Task); 100 ms-period map → one slot per **10 ms** tick (Ms10Task). `CanMap::SendByIndex` was already merged into our libopeninv (no submodule bump — the c076e19→fa6e1dc bump in PR #58 is the same libopeninv merge we already carry; verified present). **Per-message refresh is unchanged**: MAX_MESSAGES=10 and both tick ratios are exactly 10:1, so a full cycle = 10 ms (10 ms mode) / 100 ms (100 ms mode), identical to `SendAll`; `SendByIndex` no-ops on empty slots (period stays MAX_MESSAGES×tick regardless of how many are mapped). **T10 (F15) interaction checked (jotham-todo flagged this the thinky bit): net-positive.** Moving a full TX burst OFF Ms10/Ms100 *reduces* their exec time → *improves* their overrun margin, not worsens it. New task is the 3rd scheduler channel (OC3); `MAX_TASKS=4`, one spare. Overrun accounting is per-channel and unchanged; the only nuance is the *shared* `overruns` counter now also counts any Ms1 skip if `Run()` is starved >1 ms (a busy PWM ISR) — diagnostic-fidelity only, never a control-path effect, and Ms1 does a single mailbox write so it won't overrun in normal operation. No new host test (matches PR #58 — timing/HW behaviour; `AddTask`/`CheckOverrun` already host-covered). **Merged to `fixes` 2026-07-20 (call delegated by Jotham): clearly beneficial (avoids flooding the 3 bxCAN TX mailboxes on a busy dyno bus, trims the heavy tasks) and low-risk; the deploy-freeze that motivated deferral lifted, and the whole `fixes` branch is pre-HW anyway — bench validation covers it with everything else.** |

| T22 | FORK (doc) | n/a | doc only, no code | done — design note for moving the virtual-inertia control law from the supervisory host into a firmware task (the production endpoint; also the fallback if the host loop can't close over CAN). Written up as an internal doc (not part of this repo), T14 precedent. Firmware-facing decisions recorded there: spare 1 ms scheduler slot (4th channel; Ms1 stays B4's TX round-robin), `#ifdef VIRTUAL_INERTIA` flag default OFF with flag-off byte-identity (T21/C1 discipline), torque output injected at the `manualiq` summing junction (one analysed bypass path), T21's `CmdTimeout` core re-stamped by a mode-heartbeat param with ramp-to-zero (not hard-zero) on silence, param ids 173+ with every law coefficient a runtime param. Implementation gated on a host-suite plant test; no code until the bring-up timing gate rules on the host loop |

| T23 | FORK (test-only) | superproject `test/` | host suite green (59 cases / 13 suites, exit 0) after clean rebuild; no firmware code touched | done — silent-skip audit (2026-07-22, overnight). Found: `TestBrkPedal`/`TestRegen` in `test_throttle.cpp` were compiled but never listed in `REGISTER_TEST` — dead since the CalcThrottle raw-pot→normalized-percent API change (their goldens failed 5/5 when registered). Rewritten against the current throttle law (brake-pedal scaling by pot2nom incl. the never-reach-zero invariant; off-pedal regen zone incl. neutral point and pot2 scaling) and registered. `TestDualThrottle` (`#if 0`, inherited from upstream) deleted — it tests `Throttle::CheckDualThrottle`, which no longer exists anywhere in the tree. Harness hardened: `test_main.cpp` now fails (exit −1) if zero test cases are registered instead of printing "All tests passed", prints case/suite counts, and the registration list is construct-on-first-use (static-init-order hazard could silently drop registrations); Makefile links `test_*.cpp` by wildcard (hand-kept OBJS list could silently drop a test TU) and compiles test TUs with `-Werror=unused-function` (an unregistered *static* test function is now a build error — covers the TestBrkPedal defect class; it does NOT catch a definition wrapped in `#if 0` (no symbol emitted) or a non-static test function, verified empirically by adversarial review). En route: the wildcard changed suite execution order and exposed a hidden inter-suite coupling — `test_vcu`'s CanTest2 golden (regenpreset==100) silently relied on `Throttle::potmin/potmax` still being zero-init (DigitsToPercent's degenerate uncalibrated branch returns 100) i.e. on ThrottleTest not having run yet; `VCUTest::TestCaseSetup` now pins that state explicitly. libopeninv submodule test dir audited read-only (out of tonight's write scope): no unregistered/`#if 0` cases found, but it shares the harness weaknesses fixed here (zero-test silence, static-init order, hand-kept OBJS, no unused-function gate) — same treatment owed a future pass. Residuals ledgered by the adversarial pass: (1) **libopeninv `CAN_SIGNED=1` receive-map test set (~23 cases, test_canmap.cpp:663-899) is dead in every build leg** — `libopeninv/test/Makefile` defaults `CAN_SIGNED ?= 0` and neither the superproject Test target nor CI ever sets it; mitigating: default builds exclude the matching product branch too, so tests match the shipped-default config — same conditional-compilation class as the CTRL_SINE residual below, owed a CI leg or an explicit ruling when libopeninv is in scope. (2) **Dual-pot plausibility checking's successor has zero coverage**: upstream's deleted `CheckDualThrottle` cross-channel agreement check morphed into per-channel `CheckAndLimitRange` + MIN-select with four fallback branches (`vehiclecontrol.cpp:789-813`), none tested, and the ch1-bad→use-ch2 branch posts no error — candidate T24 (needs a semantics ruling before goldens, esp. whether the silent fallback is intended). (3) Harness: a registered case with zero reachable ASSERTs still counts as a pass (only the intentional `TestSetup` cases are assert-free today); ThrottleTest's setup-runs-first convention is unguarded. (4) Inherent: host suite builds CTRL_FOC only; `#if CTRL==CTRL_SINE` branches have no host coverage; `bench/adc-gate-tests` is a hardware-only rig, legitimately outside CI |

| T24 | FORK (test-only) | superproject `test/` + libopeninv `test/` | superproject host suite green (63 cases / 13 suites, exit 0); libopeninv both legs green after clean rebuild — CAN_SIGNED=1 = 122 cases / 5 suites, CAN_SIGNED=0 = 124 cases / 5 suites; no firmware/library code touched | done — closes the two T23 residuals. **Residual 2 (dual-pot successor coverage):** added 4 characterization cases to `test_vcu.cpp` (`VCUTest`) driving all four branches of `GetUserThrottleCommand`'s `POTMODE_DUALCHANNEL` select (`vehiclecontrol.cpp:789-812`) through the public `ProcessThrottle` path — both-in-range MIN-select (two sub-asserts prove it's a MIN, not "always ch1"), ch1-good/ch2-bad → use ch1 + `ERR_THROTTLE2`, ch1-bad/ch2-good → fall back to ch2, both-bad → return 0 + `ERR_THROTTLE2`. **Semantics ruling on the flagged "silent fallback": it is NOT silent** — the `!inRange1` guard *upstream* of the select (line 746-750) sets `err_out` and posts `ERR_THROTTLE1` unconditionally, so a bad channel 1 always raises a fault; the select's per-channel error asymmetry (ch2-bad→`ERR_THROTTLE2`, ch1-bad→`ERR_THROTTLE1`) reads as intentional (per-channel ids) and is **locked as characterization, firmware behaviour unchanged**. Rig makes the chain a pass-through for a positive command (`brknom=0`+`linearity=1` → identity `CalcThrottle`; ramp rate 200 spans `[-100,100]` in one call so the private persistent `throttleRamped` can't perturb goldens; every derate configured wide) so `Param::potnom` == the selected channel's percent; a sentinel `errorMessage` reset isolates `ProcessThrottle`'s posts from `HandleRx`. Driven via `POTMODE_DUALCHANNEL\|POTMODE_CAN` with `opmode=MOD_RUN` so `PostErrorIfRunning` fires. **Residual 1 (libopeninv `CAN_SIGNED=1` dead test set):** root cause = the superproject `Test` target built `libopeninv/test` with the Makefile default `CAN_SIGNED=0` only, so the ~23 signed-reception cases (`test_canmap.cpp:663-899`) never ran in the *aggregate* suite (libopeninv's OWN `CI-build.yml` already runs both legs — the gap was superproject-only). Fix: `Test` now builds **and runs** both `CAN_SIGNED=1` and `=0` legs (shipped-default `=0` leg built last so CI's trailing `test_libopeninv` sees it); either leg failing fails `make Test` = the CI leg the residual asked for. Ported the T23 harness hardening (zero-test guard + case/suite counts + construct-on-first-use) to `libopeninv/test/test_main.cpp` (**separate libopeninv commit `5328371` on `fixes`**) so the signed leg is auditable ("122 cases") and can't silently register zero. **✅ Submodule discipline (RESOLVED 2026-07-28): superproject pointer is bumped to libopeninv `5328371`. That push debt is now DISCHARGED — verified against the live remote (`git ls-remote fork fixes` → `5328371f70e0…	refs/heads/fixes`), so the superproject no longer references an unpushed libopeninv SHA. Historical note: this row previously carried an OWED warning (`git -C libopeninv push fork fixes`); the push has since happened. The G8/T25 merges of 2026-07-28 did NOT move either submodule pointer (libopeninv `5328371`, libopencm3 `5ba1bb5` unchanged), so no submodule-first push ordering is required for them.** Residuals still open after T24: (a) the *other* libopeninv harness weaknesses T23 flagged — hand-kept OBJS list (can drop a test TU) and the missing `-Werror=unused-function` gate — NOT ported (superproject `test/` already has both; libopeninv `test/` still lacks them), owed a proper libopeninv push-cycle pass; (b) T23 residuals 3 (a zero-`ASSERT` case still counts as a pass) and 4 (`CTRL_SINE` branches / bench-only rigs) unchanged; (c) **plausibility delta = candidate T25 (firmware, not test-only):** upstream's `CheckDualThrottle` cross-channel *agreement* check is fully gone — the successor only range-checks each channel independently then MIN-selects, so two channels that are *both individually in range but disagree* (e.g. one stuck at a plausible value) are silently reconciled to the lower reading with no cross-check/error. MIN-select gives partial fail-safety (a channel stuck HIGH is masked; stuck LOW drags the command down), but a dedicated agreement/plausibility guard would need a firmware change + semantics ruling — deliberately out of this test-only pass.

| T25 | FORK (**merged to `fixes` 2026-07-28**) | superproject | on-branch: host suite 63→68 (13 suites, exit 0); ARM FOC + SINE builds. Post-merge on `fixes` (aggregate with G8): host suite **76 cases / 14 suites, exit 0**; ARM FOC + SINE builds both rc=0; **NO HW PASS** | on `feat/T25-dualpot-plausibility` (`de15cf6`, off `fixes` @ `02fa4f9`, **not pushed**). **RULED-ACCEPTED (b) 2026-07-23** — supersedes the earlier REJECTED note. T24 residual (c): the DUALCHANNEL successor to upstream's deleted `CheckDualThrottle` range-checks each channel then MIN-selects with no cross-channel agreement test, so two channels both individually in range but disagreeing (one stuck at a plausible value) are silently reconciled to the lower reading. Ruling option (b): **keep the MIN-select command unchanged, ADDITIONALLY raise a plausibility fault** so a stuck-but-in-range pot is never silent. New param **`potdiffmax`** (unit %, 0–100, **default 0 = check DISABLED → shipped-default binary behaviourally unchanged**; when >0, a normalized channel disagreement strictly greater than `potdiffmax` raises a **dedicated** fault **`ERR_THROTTLEDIFF`**); **id 201** (skips B2 167-171, T21 172, G8 200; leaves 173-199 for T22 virtual-inertia coeffs — param-id header tracker updated). **Dedicated error id ruled 2026-07-23 (superseded my residual):** a dual-channel disagreement is its own fault class, distinct from the per-channel `ERR_THROTTLE1`/`ERR_THROTTLE2` range faults, so it gets its own id instead of reusing `ERR_THROTTLE1` (upstream's choice). New `ERROR_MESSAGE_ENTRY(THROTTLEDIFF, ERROR_DISPLAY)` **appended at the END** of `errormessage_prj.h`'s list so every existing error ordinal stays stable (errors may be logged/displayed numerically); display string auto-derived from the id name by `errormessage.cpp`'s stringify macro (no separate string table). Type `ERROR_DISPLAY` matches the other throttle faults (flag, not hard stop). Tolerance-on-normalized-percent + strict-`>` semantics still **mirror upstream `CheckDualThrottle`** (hardcoded 10-pt tolerance); **deviations from upstream: (a)** dedicated id vs upstream's `ERR_THROTTLE1`, **(b)** upstream also zeroed the command (`*potval = potmin[0]`) — we keep the MIN-select per the ruling and only flag. Decision logic pure in `include/dualpotplausible.h` (`cmdtimeout.h`/`manualclamp.h` precedent) so the host suite drives the real check; it runs ONLY inside the both-in-range branch, so out-of-range channels short-circuit to the existing per-channel range faults untouched. Host tests (`test/test_vcu.cpp` VCUTest, +5): below-tolerance no-op + MIN, above-tolerance `ERR_THROTTLEDIFF` + MIN still commanded, strict-`>` boundary (diff==tol plausible, tol−1 trips), disabled-default no-op at large disagreement, and range-fault interaction (check ON but ch2 out-of-range → `ERR_THROTTLE2`, asserts `ERR_THROTTLEDIFF` is distinguishable from `ERR_THROTTLE1`/`ERR_THROTTLE2`). Existing 4 T24 characterization cases stay green (potdiffmax=0 = disabled). **Merged to `fixes` 2026-07-28 as merge commit `b925c1e`** (authorized by Jotham 2026-07-27; branch tip verified == authorized `de15cf6` immediately before the merge). Sole conflict was the param-id bookkeeping comment (`param_prj.h:27`) — both branches edited that one tracker line; resolved as a union (T22's 173-199 reservation + `200 = G8 manualiqmax, FOC-only` + `201 = T25 potdiffmax`), dropping the now-stale "on feat/T25" branch qualifier. No code conflict. Default-OFF re-verified on the merged tree: `param_prj.h:131` `PARAM_ENTRY(CAT_THROTTLE,potdiffmax, "%", 0, 100, 0, 201 )` (default **0**) gates `dualpotplausible.h:54` `if (tolerance <= 0) return false;` — the check is inert on the shipped default |

| G8 | FORK (**merged to `fixes` 2026-07-28**) | superproject | on-branch: host suite 63→71 (14 suites, exit 0); ARM FOC + SINE builds (SINE bin md5-identical to baseline, FOC-only); both libopeninv CAN_SIGNED legs unchanged (122/124). Post-merge on `fixes` (aggregate with T25): host suite **76 cases / 14 suites, exit 0**; ARM FOC + SINE builds both rc=0; **NO HW PASS** | on `feat/G8-manualiq-clamp` (`a09ab5e`, off `fixes` @ `04b51fa`, single squashed commit, **not pushed**) — FMEA **G8** firmware-side magnitude clamp on the CAN **manual** current path (`manualiq`/`manualid`) (defends ABS-1 / RTL-2). Defence-in-depth OUTSIDE the CM4 failure boundary: today the only firmware bound is the ±400 A param range; the host `i_max_burst_a` clamp is attested at ARM (console G6) but the FMEA requires a firmware ceiling that **a single random host fault cannot casually defeat**. New FOC-only spot param **`manualiqmax`** (unit A, 0–400, **default 400 = the existing range, so the shipped-default binary is behaviourally unchanged** until configured down); **id 200** — skips B2's 167-171 + T21's 172, leaves 173-199 to T22's virtual-inertia coeffs. Added via a `G8_CLAMP_PARAM` macro *outside* PARAM_LIST (a block comment truncates the backslash-continued list). Clamp applied ONCE per control step at the top of the FOC summing-junction block (`pwmgeneration-foc.cpp`), bounding BOTH manual current magnitudes before either reaches a controller `SetRef`, in the ZOE and non-ZOE branches. Clamp + effective-ceiling logic pure in `include/manualclamp.h` (cmdtimeout / RegenTaperHold precedent) so the host suite drives the real logic; Param/latch glue firmware-compile-checked. **Passed two adversarial Fable reviews (2026-07-23); these blockers were fixed:** **(F1 persistence)** `manualiqmax` is a persisted **`PARAM_ENTRY` (TYPE_PARAM)**, not a TESTP_ENTRY — `param_save.cpp` persists only TYPE_PARAM, so a TESTPARAM would reset to the 400 default (= clamp off) every power cycle until the host rewrote it; **(F2 raise-proofing)** a **boot latch** bounds the host-settable ceiling: effective ceiling = **MIN(live param, value latched once at init after flash load)** via `PwmGeneration::LatchManualCurrentCeiling()` — a runtime **lower** takes effect immediately, a runtime **raise** does not: raising requires the ordered chain **SDO-write id 200 → `SDO_CMD_SAVE` → `SDO_CMD_RESET`** (reboot re-latches at the new value), i.e. 3 well-formed frames + a power cycle, which a single random host fault cannot execute; **(V2-1 latch ordering)** the latch is captured **immediately after `parm_load()`, BEFORE** the `Stm32Can`/`CanMap`/`InverterSdo` constructors bring CAN RX live (CanMap's ctor loads flash-stored RX maps possibly targeting id 200) — latching after CAN-live left a ~0.5–5 ms window where a host spamming `manualiqmax` during power-cycle could poison the session's latch; call site carries a do-not-reorder comment (`UpgradeParameters`/`Param::Change` verified not to touch `manualiqmax`); **(F3 scope)** claim is scoped to the **manual** current path ONLY — the throttle path (`throtcur`/`potmode`) stays a host-reachable uncapped current command, noted as a possible **G8b** follow-up (same persist+latch treatment). **ACCEPTED RESIDUAL (orchestrator ruling):** the write→save→reset raise chain **IS host-automatable** (an adversarial host is outside G8's single-fault model); gating SDO save/reset in RUN was **considered and rejected** (breaks legitimate remote commissioning). G8 raises the bar from "one bad frame" to "ordered multi-step + reboot"; it does not claim host-proof. Host tests (`test/test_manualclamp.cpp`, **8 cases**): default-400 pass-through, clamp-active both signs on iq and on id, zero/negative-ceiling defensiveness, **latch blocks a runtime raise, latch allows a runtime lower, re-latch applies a saved raise**, and the shared `Param::Set` range guard (pinned on `ampnom` since `manualiqmax` is FOC-only and absent from the SINE param branch the host links). Residuals: **(a)** the latch capture-ordering is a firmware-boot invariant the host suite **cannot** exercise → **bench-verify item**; **(b)** the `isIdle` standstill check still reads raw manualiq/manualid (gates offset-cal only, not torque authority — deliberate). **Merged to `fixes` 2026-07-28 as merge commit `c91de6a`** (authorized by Jotham 2026-07-27; branch tip verified == authorized `a09ab5e` immediately before the merge; merged clean, no conflicts — the `param_prj.h:27` tracker-line conflict arose only on the T25 merge that followed). Default-OFF re-verified on the merged tree: `param_prj.h:270` `PARAM_ENTRY(CAT_TEST, manualiqmax, "A", 0, 400, 400, 200 )` (default **400**) and the pre-latch initializer `pwmgeneration-foc.cpp:57` `static s32fp manualIqMaxLatch = FP_FROMINT(400);`. **Precision on "default OFF" (differs from T21/T25):** G8 has NO compile flag and NO disable sentinel — `ClampManualCurrent` executes on every FOC control step unconditionally. It is *behaviourally* inert at the default because the ceiling 400 A equals the full `manualiq`/`manualid` TESTP_ENTRY range (±400, `param_prj.h:304-305`), so the clamp can never bind until commissioned downward. Bench-gated in the sense of "ships with no authority reduction", not "ships with the code path disabled". **⚖️ RULED 2026-07-28 (Jotham, accepting Fable rec): NO disable flag — inertness-by-value IS the designed OFF for this limiter.** Rationale: `manualiq` is the rig's primary drive path and G8 defends against the very CAN channel a disable knob would ride — a sentinel/flag is either host-settable (the distrusted channel can switch the protection off) or needs its own latch (complexity for negative value); an enable-flag shipping OFF = silent-unprotected state, worse under fail-closed posture than 6 lines of pure, host-suite-driven identity arithmetic in the path. T25-style sentinels remain correct for *detectors* (false-trip risk); not for limiters. **Bench item (adds to residual (a)):** first powered manual-command session commissions `manualiqmax` down to a bench-safe ceiling via the full chain (set → flash save → power cycle re-latch) and verifies the latch ordering behaves as designed |

| G8b | FORK (**merged to `fixes` 2026-07-28**, merge `a7df40e` after orchestrator review PASSED — seam/latch/default-exactness/FOC-scope/flash-audit all verified; NaN pass-through noted as pre-existing non-issue, no upstream NaN pathway; float-helper duplication upheld over editing the closed G8 artifact) | superproject | on-branch: host suite **76→84 cases, 14→15 suites, exit 0**; ARM `make CONTROL=FOC` and `make CONTROL=SINE` both **rc=0** (clean between switches — the Makefile does not track `CONTROL=`); **SINE bin md5-identical to the branch base `e1dd77b`** (`537d0a022c9735064b75ef1ee59aa865`), which is the mechanical proof that the change is FOC-only; **NO HW PASS** | on `feat/G8b-throttle-clamp` (off `fixes` @ `e1dd77b`, **not pushed**) — closes **G8's own F3 scope residual**: G8 capped the CAN `manualiq`/`manualid` path and explicitly recorded the throttle path as still "a host-reachable uncapped current command", flagged as a possible G8b follow-up with the same persist+latch treatment. **The gap, sized:** in the FOC build the throttle percent becomes a stator current request as `is = throtcur [A/%] * torquePercent [%]` (`pwmgeneration-foc.cpp:200-217`, `SetTorquePercent`). `throtcur` is a persisted param settable over the SAME CAN/SDO path as everything else, its range is 0–10 A/%, and the throttle percent is bounded to ±100 — so the only pre-G8b firmware bound on the commanded stator current magnitude was **1000 A, i.e. 2.5× the ±400 A `manualiq` range that G8 thought worth capping**. **Seam chosen: `is` BEFORE `FOC::Mtpa` splits it** (`src/pwmgeneration-foc.cpp:203-215`). Mtpa is magnitude-preserving by construction (`idref^2 + iqref^2 == is^2`, `libopeninv/src/foc.cpp:98-105`), so bounding `\|is\|` bounds the current that actually reaches the controllers. Clamping `id`/`iq` separately *after* the split would NOT bound the vector magnitude; clamping the throttle **percent** would leave `throtcur` free to scale past the ceiling — the ceiling caps the RESULTING current, both polarities symmetric (drive and regen alike), exactly as in G8. New FOC-only param **`throtcurmax`** (unit A, 0–1000, **default 1000 = the existing reachable full range (`throtcur` max 10 A/% × throttle magnitude 100%), so the shipped-default binary is behaviourally unchanged** until configured down); **id 202** — follows G8's 200 and T25's 201, still leaving 173-199 to T22's virtual-inertia coeffs (param-id header tracker updated). Placed **inside `THROTTLE_PARAMETERS_FOC`** (already FOC-only, already the throttle's home) rather than a G8-style standalone `*_PARAM` macro; the block comment sits ABOVE the `#define` since it cannot live inside the backslash-continued macro body. Clamp + effective-ceiling logic pure in **`include/throtclamp.h`** (cmdtimeout / manualclamp / dualpotplausible precedent) so the host suite drives the real logic; Param/latch glue firmware-compile-checked. **All three G8 adversarial-review fixes replicated verbatim in intent:** **(F1 persistence)** `throtcurmax` is a persisted **`PARAM_ENTRY` (TYPE_PARAM)**, not a TESTP_ENTRY — `param_save.cpp` persists only TYPE_PARAM, so a TESTPARAM would reset to the 1000 default (= clamp off) every power cycle until the host rewrote it; **(F2 raise-proofing)** boot latch bounds the host-settable ceiling: effective ceiling = **MIN(live param, value latched once at init after flash load)** via `PwmGeneration::LatchThrottleCurrentCeiling()` — a runtime **lower** takes effect immediately, a runtime **raise** does not (requires SDO-write id 202 → `SDO_CMD_SAVE` → `SDO_CMD_RESET`); **(V2-1 latch ordering)** the latch is captured **immediately after `parm_load()`, BEFORE** the `Stm32Can`/`CanMap`/`InverterSdo` constructors bring CAN RX live, back-to-back with G8's latch inside the same `#if CONTROL == CTRL_FOC` block (`src/stm32_sine.cpp:450-467`), carrying a do-not-reorder comment noting a flash-stored CAN RX map could target id 202 as easily as id 200. **FOC/SINE scoping answer (asked, and it is honestly FOC-only):** `throtcur` is declared only in `THROTTLE_PARAMETERS_FOC` and referenced only from `pwmgeneration-foc.cpp`; the SINE build's `SetTorquePercent` (`src/pwmgeneration-sine.cpp:86+`) produces an **amplitude/slip (voltage)** command, not a current command, so there is **no throttle→current seam to clamp in SINE** — same scoping as G8, for the same structural reason, and evidenced rather than asserted by the md5-identical SINE binary. **⚠ Flash slot audit (performed as the budget note below demands, since this adds a persisted param):** measured on the FOC build by enumerating `Param::GetType(...) == TYPE_PARAM` — base `e1dd77b`: 100 persisted params, highest TYPE_PARAM enum index **102** (`manualiqmax`), 24 free slots; with G8b: **101 persisted params, `throtcurmax` at enum index 48, highest TYPE_PARAM index 103, 23 free slots** against the `NUM_PARAMS = 127` bound. Headroom is fine and T22's precondition is unchanged (it just loses one slot). **Mid-list insertion is safe for already-flashed boards:** `parm_load` resolves every saved slot through `Param::NumFromId(key)` (`param_save.cpp:113`), i.e. persistence is keyed by the stable param **id**, not the enum index — so shifting ~55 later params' indices cannot misread an existing flash page, and a pre-G8b board upgraded to G8b firmware simply finds no slot for id 202 and takes the 1000 default (= clamp inert). Host tests (`test/test_throtclamp.cpp`, **8 cases**, new `ThrotClampTest` suite): default-1000 pass-through (incl. the pre-G8b worst case 10 A/% × 100%), clamp-active both signs, the real `throtcur × percent` arithmetic with a commissioned 120 A ceiling binding drive AND regen while ordinary throttle is untouched, zero/negative-ceiling defensiveness, **latch blocks a runtime raise, latch allows a runtime lower, re-latch applies a saved raise**, and the shared `Param::Set` range guard (pinned on `ampnom` since `throtcurmax` is FOC-only and absent from the SINE param branch the host suite links — the harness defines `CTRL`, not `CONTROL`, so `param_prj.h` takes its SINE branch). **⚖️ Default-OFF mechanism is inertness-by-value, per the 2026-07-28 Jotham ruling recorded in the G8 row: NO compile flag, NO disable sentinel.** `ClampThrottleCurrent` executes unconditionally on every `SetTorquePercent` call and is *behaviourally* inert at the default because 1000 A equals the full reachable `throtcur × percent` product. Same reasoning as G8: this is a **limiter**, not a detector, and a disable knob would ride the very CAN channel the clamp distrusts. **⚠ DEVIATION from G8's pattern (flagged, not improvised):** the throttle path is **`float`** end to end (`Param::GetFloat` → `FOC::Mtpa`), whereas G8's clamp is `s32fp`/`int32_t`. Rather than convert to fixed point purely to reuse `ClampManualCurrent` (which would add a rounding step in the torque path for no benefit), G8b gets its **own typed helper pair in a sibling header**, `ClampThrottleCurrent`/`EffectiveThrottleCeiling`, with the same 4-line arithmetic in float. **This deliberately duplicates ~8 lines of trivial min/max logic across `manualclamp.h` and `throtclamp.h`.** The DRY alternative — a `template<typename T>` pair in a shared `currentclamp.h` with both headers delegating — was considered and **rejected on the grounds that it would edit `manualclamp.h`, a closed, twice-adversarially-reviewed G8 artifact, for a cosmetic gain**. Orchestrator: if you'd rather have the template, it is a ~15-minute mechanical change and both suites re-prove it. Residuals: **(a)** the latch capture-ordering is a firmware-boot invariant the host suite **cannot** exercise → **bench-verify item**, identical in kind to G8 residual (a) and verifiable in the same bench session; **(b)** the write→save→reset raise chain **IS host-automatable** — same ACCEPTED residual as G8, an adversarial host is outside the single-fault model; **(c)** `throtcur` itself remains freely host-writable — G8b bounds the *product*, not the scale factor, which is the correct place to bound it but means a `throtcur` write still changes pedal feel below the ceiling; **(d)** everything upstream of the seam (regen ramps, `regenTaperHold`, every derate/limit in `ProcessThrottle`) is untouched by design and still shapes `torquePercent` exactly as before; **(e)** `isIdle`'s standstill check still reads the raw `torquePercent`/manual commands (gates offset-cal only, not torque authority — deliberate, mirrors G8 residual (b)). **Bench item:** the first powered throttle session commissions `throtcurmax` down to a bench-safe ceiling via the full chain (set → flash save → power cycle re-latch) and verifies the latch ordering behaves as designed — fold into G8's existing commissioning item, one session covers both ceilings |

**⚠ Flash save-slot budget (T22 PRECONDITION, surfaced by G8 review 2026-07-23).**
The param flash page holds `NUM_PARAMS = (1024−8)/8 = 127` save slots (the
`param_save.cpp` save/load loops are bounded by `NUM_PARAMS`). **Measured on the
FOC build 2026-07-28 (G8b branch): 101 persisted params, highest TYPE_PARAM enum
index 103 (`manualiqmax`), 23 free slots** — G8b's `throtcurmax` took one slot
(base `e1dd77b` was 100 params / index 102 / 24 free). Note the *index* is what
`parm_save` bounds, so inserting a param mid-list shifts every later param's
index; that is safe for existing flash pages because `parm_load` resolves slots
by param **id** via `Param::NumFromId`, not by index. **T22's planned ~27
virtual-inertia params (ids 173+) would push the later params past the
save-loop bound and SILENTLY stop persisting** — no error, they'd just reset to
default every boot (the exact class of bug G8's F1 fixed for one param). T22
must first add a second flash page or lift the save-bound before its params land.
Applies to *any* future param block, not just T22 — audit the slot count before
adding more persisted params.

Baseline pins → integration branch: superproject `1dfab85 → fixes`,
libopeninv `78e3f72 → fixes`.

(Append one row per task as branches merge to `fixes`.)
